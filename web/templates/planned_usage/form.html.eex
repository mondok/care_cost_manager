<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :size, "Size", class: "control-label" %>
    <%= text_input f, :size, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :shift, "Shift", class: "control-label" %>
    <%= text_input f, :shift, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :usage, "Usage", class: "control-label" %>
    <%= text_input f, :usage, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :transaction_date, "Transaction date", class: "control-label" %>
    <%= datetime_select f, :transaction_date, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
